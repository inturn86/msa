server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: C:\IntelliJ Projects\msa\natvie-file-repo
        git:
          #git 연동
          uri: https://github.com/inturn86/spring_cloud_config.git
